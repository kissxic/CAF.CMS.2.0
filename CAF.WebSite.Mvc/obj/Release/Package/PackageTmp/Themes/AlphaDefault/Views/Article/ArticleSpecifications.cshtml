@model IList<ProductSpecificationModel>

@if (Model.Count > 0)
{
    for (int i = 0; i < Model.Count; i++)
    {
        var item = Model[i];
        <li><p>【@item.SpecificationAttributeName】@item.SpecificationAttributeOption</p></li>
    }


}